# Installers (bugtest)

Gate password: `froge`

| Platform | File | Notes |
|----------|------|--------|
| macOS | [EndoASD-mac-Bugtest.dmg](./EndoASD-mac-Bugtest.dmg) | Open DMG → drag **Endo ASD.app** to Applications |
| Windows | [EndoASD-Windows-Bugtest.zip](./EndoASD-Windows-Bugtest.zip) | Unzip → run `EndoASD.bat` (needs [Python 3](https://www.python.org/downloads/)). True `EndoASD.exe` / Setup builds on Windows via `build\build-windows.bat` or the **Build Windows app** GitHub Action. |

Build: see `update-channel.json`.
