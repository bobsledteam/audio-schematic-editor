name: Build macOS app

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PyInstaller
        run: python -m pip install -r requirements-build.txt

      - name: Build app bundle
        run: python -m PyInstaller build/GuitarWiring-mac.spec --clean --noconfirm

      - name: Create DMG
        run: |
          hdiutil create \
            -volname "Guitar Wiring" \
            -srcfolder "dist/Guitar Wiring.app" \
            -ov -format UDZO \
            dist/GuitarWiring.dmg

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: GuitarWiring-mac
          path: |
            dist/Guitar Wiring.app
            dist/GuitarWiring.dmg
